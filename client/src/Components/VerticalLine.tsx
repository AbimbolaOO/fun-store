import styled from '@emotion/styled';
const VerticalLine = styled.div`
  width: 1px;
  height: 16px;
  background-color: ${({ theme }) => theme.palette.verticalLineColor};
`;

export default VerticalLine;
